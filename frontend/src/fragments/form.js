import styled from 'styled-components';
import {
	space,
	layout
} from 'styled-system';

const form = styled.form`
	margin: 0px;
	padding: 0px;
	width: 100%;
	${space}
	${layout}
`

export default form;